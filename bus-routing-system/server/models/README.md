This folder is reserved for model-layer query abstractions.

In this starter project, query logic is currently implemented inside controllers for clarity.
You can progressively move SQL statements into dedicated model files (`userModel.js`, `tripModel.js`, etc.) as the codebase grows.
